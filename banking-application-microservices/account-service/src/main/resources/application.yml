server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/accountdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
  
  jpa:
    hibernate:
      ddl-auto: update  # Auto-create/update tables from entities
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        
  # File Upload Configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
      
  # Flyway Migration  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: noreplyinterbankinghub@gmail.com
    password: sufd mtak snum ahgc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Admin Service Configuration
admin:
  service:
    url: http://localhost:8083
