server:
  port: 8082

spring:
  application:
    name: bank-service
  datasource:
    url: jdbc:mysql://localhost:3306/bankdb?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
